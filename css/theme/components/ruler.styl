.ruler
    display: flex
    justify-content: flex-start
    align-items: center
    margin-top: -16px

    .ico
        width: 55px
        height: 55px
        color: var(--tertiary)
        opacity: .5

    span
        padding-left: 10px
        color: var(--primary)
        text-transform: uppercase
        font-weight: bold
        font-size: 12px
